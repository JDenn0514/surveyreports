% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool-pvals.R
\name{pool_pvals}
\alias{pool_pvals}
\title{Pool p-values across a list of analysis results and apply a single
family-wise multiplicity correction}
\usage{
pool_pvals(
  results,
  method = "BH",
  p_col = "p_value",
  new_col = "p_value_adj",
  id_col = "source",
  strip_within_adj = FALSE
)
}
\arguments{
\item{results}{A list (named or unnamed) of tibbles / data frames.
Every element must contain a column named per \code{p_col}. Length
must be at least one. List elements may have heterogeneous column
sets; missing columns are NA-filled on bind.}

\item{method}{Character(1). Adjustment method, passed verbatim to
\code{stats::p.adjust()}. One of \code{stats::p.adjust.methods}. Default
\code{"BH"}.}

\item{p_col}{Character(1). Name of the column holding raw p-values
to be pooled. Default \code{"p_value"}. Must exist in every list
element.}

\item{new_col}{Character(1). Name of the column where pooled-
adjusted p-values are written. Default \code{"p_value_adj"}.}

\item{id_col}{Character(1). Name of the source-identifier column
added to the bound output. Default \code{"source"}. Filled from list
names; for unnamed elements the integer index is coerced to
character (matching \code{dplyr::bind_rows(.id = ...)} semantics).}

\item{strip_within_adj}{Logical(1). If \code{FALSE} (default) and any
input contains a column named per \code{new_col}, that column is
renamed to \code{paste0(new_col, "_within")} per element before
binding, and a warning is emitted. If \code{TRUE}, any pre-existing
\code{new_col} column is silently dropped from each element before
binding; no warning.}
}
\value{
An object of S3 class \code{c("survey_pooled_pvals", "tbl_df", "tbl", "data.frame")} -- a tibble with an additional class tag.
Column ordering: (1) the union of input columns in input-union
order; (2) \code{id_col}; (3) \code{new_col}; (4) \code{paste0(new_col, "_within")} if applicable. The result carries a \code{.meta} attribute
(a list) with keys \code{method}, \code{family_size}, \code{n_total}, \code{n_na},
\code{n_significant_05}, \code{id_col}, \code{p_col}, and \code{new_col}.
}
\description{
Row-binds a list of analysis result tibbles (each carrying a
\code{p_value} column produced by \code{surveycore::get_diffs()},
\code{surveycore::get_t_test()}, \code{surveycore::get_pairwise()},
\code{surveycore::get_anova()}, \code{surveycore::clean.survey_glm_fit()}, or
any future \verb{get_*()} carrying a \code{p_value} column) into a single
family and applies \code{stats::p.adjust()} once across the entire pool.
}
\details{
\subsection{Method choice}{

\code{pool_pvals()} dispatches verbatim to \code{stats::p.adjust()}; the
adjustment formula is determined entirely by \code{method}.
\itemize{
\item \code{"BH"} (Benjamini-Hochberg, 1995) -- controls the false discovery
rate (FDR) under independence or positive regression dependency
(PRDS). Recommended default for multi-DV survey families.
\item \code{"BY"} (Benjamini-Yekutieli, 2001) -- controls FDR under arbitrary
dependence; strictly more conservative than BH. Use when the
dependence structure across DVs is unknown or arbitrary.
\item \code{"fdr"} -- alias for \code{"BH"} per \code{stats::p.adjust()} source. The
two are identical.
\item \code{"holm"} (Holm, 1979) -- controls the family-wise error rate
(FWER) under arbitrary dependence. Uniformly more powerful than
Bonferroni.
\item \code{"hochberg"} (Hochberg, 1988) -- controls FWER, requires
independence or MTP_2 dependence (not just PRDS). Use Holm
instead when DVs are correlated and FWER control is desired.
\item \code{"hommel"} (Hommel, 1988) -- controls FWER, requires independence
or MTP_2 dependence. Same caveat as Hochberg.
\item \code{"bonferroni"} -- controls FWER under any dependence structure
(Bonferroni's inequality). Most conservative of the FWER
procedures; preferred for tiny families.
\item \code{"none"} -- pass-through; raw p-values returned in \code{new_col}.
}
}

\subsection{Default method}{

The default \code{method = "BH"} reflects the typical structure of
survey p-values across DVs, which are usually positively dependent
(PRDS) but rarely independent. When the dependence structure is
unknown or known to be arbitrary, switch to \code{method = "BY"} for a
robust FDR-controlling alternative.
}

\subsection{Recommended workflow}{

Set \code{pval_adj = NULL} on every upstream \verb{get_*()} call when planning
to pool, so within-call adjustment is not produced and
\code{pool_pvals()} applies the global correction once. Note that
attributes carried on input tibbles (e.g., \code{variable_label} from
\code{get_diffs()} results) are NOT preserved on the output; this matches
\code{dplyr::bind_rows()} default behavior. Users who need attribute
preservation must re-attach the attribute after pooling.
}

\subsection{Statistical caveat about double-adjustment}{

surveycore's \code{get_t_test()}, \code{get_pairwise()} (default
\code{pval_adj = "holm"}), and \code{get_diffs()} overwrite the \code{p_value}
column in place when \code{pval_adj != NULL}. \code{pool_pvals()} cannot
detect this by column inspection alone -- the
\code{surveyreports_warning_pool_pvals_input_pre_adjusted} warning class
only fires when a separate column matching \code{new_col} is present.
Pass \code{pval_adj = NULL} upstream to avoid silent double-adjustment.
}

\subsection{Worked NA-denominator example}{

With pooled p-values \verb{(0.01, 0.02, 0.05, NA, NA)} and
\code{method = "BH"}, the BH denominator is \code{n = sum(!is.na(p)) = 3}
(not 5); adjusted values are \verb{(0.03, 0.03, 0.05, NA, NA)}.
}

\subsection{Column-shape user-owned risk}{

Column shapes and the semantic coherence of any \code{group} column
must be checked by the user; \code{pool_pvals()} does not validate
semantic coherence across input elements.
}

\subsection{Cross-design pooling caveat}{

When results come from different surveys, sampling frames, or
weighting schemes, the user is responsible for ensuring
exchangeability across the pool.
}

\subsection{Small-m regime}{

BH and related step-up methods become very conservative when
\code{m < 5} (the family size). Consider \code{method = "bonferroni"} or
unadjusted-with-exploratory-framing for tiny families.
}

\subsection{\code{"fdr"} alias note}{

\code{"fdr"} is a dispatch alias for \code{"BH"} per \code{stats::p.adjust()}
source; the two produce identical output.
}

\subsection{Discrete p-value caveat}{

Classical BH assumes continuous p-values. Chi-square tests of
small contingency tables can produce ties that violate this. See
the \code{DiscreteFDR} package on CRAN for that regime.
}

\subsection{Collection workflow}{

Users with results from a collection-dispatched \verb{get_*()} call (a
single tibble carrying a \code{.id} / \code{.survey} column) should split
into per-\code{.id} tibbles via \code{dplyr::group_split()} before passing
to \code{pool_pvals()}.
}

\subsection{See also}{

Storey (2002) q-values, Romano-Wolf (2005) bootstrap stepdown, and
the \code{multcomp} / \code{mutoss} packages provide multiplicity machinery
surveyreports does not implement.
}
}
\examples{
df <- data.frame(
  y1 = c(1.2, 0.8, 2.1, 1.5, 0.9, 1.8),
  y2 = c(2.3, 1.1, 2.8, 0.7, 1.9, 2.2),
  sex = c("M", "F", "M", "F", "M", "F"),
  wt = c(1.1, 0.9, 1.2, 0.8, 1.0, 1.1)
)
d <- surveycore::as_survey(df, weights = wt)
r1 <- surveycore::get_diffs(d, y1, sex, pval_adj = NULL)
r2 <- surveycore::get_diffs(d, y2, sex, pval_adj = NULL)
pool_pvals(list(y1 = r1, y2 = r2))

}
\references{
Benjamini, Y. and Hochberg, Y. (1995). Controlling the False
Discovery Rate: A Practical and Powerful Approach to Multiple
Testing. \emph{Journal of the Royal Statistical Society, Series B}
57(1), 289-300. \doi{10.1111/j.2517-6161.1995.tb02031.x}

Benjamini, Y. and Yekutieli, D. (2001). The control of the false
discovery rate in multiple testing under dependency. \emph{Annals of
Statistics} 29(4), 1165-1188. \doi{10.1214/aos/1013699998}

Holm, S. (1979). A simple sequentially rejective multiple test
procedure. \emph{Scandinavian Journal of Statistics} 6(2), 65-70.

Hochberg, Y. (1988). A sharper Bonferroni procedure for multiple
tests of significance. \emph{Biometrika} 75(4), 800-802.
\doi{10.1093/biomet/75.4.800}

Hommel, G. (1988). A stagewise rejective multiple test procedure
based on a modified Bonferroni test. \emph{Biometrika} 75(2), 383-386.
\doi{10.1093/biomet/75.2.383}
}
