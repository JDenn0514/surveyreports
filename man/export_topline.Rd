% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-topline.R
\name{export_topline}
\alias{export_topline}
\title{Export a topline frequency table to an Excel workbook}
\usage{
export_topline(
  design,
  vars,
  file_name,
  conf_level = 0.95,
  decimals = 1L,
  variance = NULL,
  show_n = TRUE,
  show_eff_n = FALSE
)
}
\arguments{
\item{design}{A survey design object created by
\code{\link[surveycore:as_survey]{surveycore::as_survey()}}, \code{\link[surveycore:as_survey_replicate]{surveycore::as_survey_replicate()}}, or
\code{\link[surveycore:as_survey_twophase]{surveycore::as_survey_twophase()}}. A \link[surveycore:survey_collection]{surveycore::survey_collection}
is also accepted; the output will include a pooled "Total" column and
one column per wave.}

\item{vars}{<\code{\link[tidyselect:language]{tidy-select}}> Variable(s) to
tabulate. Use bare column names (\code{q1}), \code{c(q1, q2)}, or tidyselect
helpers such as \code{starts_with("q")}.}

\item{file_name}{Path to the output \code{.xlsx} file. Must end in \code{.xlsx}.}

\item{conf_level}{Numeric scalar in \verb{(0, 1)}. Confidence level for
intervals when \code{variance = "ci"}. Default \code{0.95}.}

\item{decimals}{Positive integer. Number of decimal places for displayed
percentages. Default \code{1L}.}

\item{variance}{One of \code{NULL}, \code{"se"}, or \code{"ci"}. Controls whether
standard errors or confidence intervals are included in the output.
Default \code{NULL} (neither).}

\item{show_n}{Logical. Whether to include an unweighted count column.
Default \code{TRUE}.}

\item{show_eff_n}{Logical. Whether to display the effective N in the
percentage column header. Default \code{FALSE}.}
}
\value{
\code{invisible(file_name)} — the path supplied in \code{file_name}.
}
\description{
Produces a publication-quality topline frequency table for one or more
survey variables, written to a styled \code{.xlsx} workbook. Each variable is
auto-classified (single-choice, SATA, or battery) via
\code{\link[surveycore:classify_question_type]{surveycore::classify_question_type()}} and rendered to its appropriate
table block on a single sheet named \code{"Topline"}.
}
\examples{
\dontrun{
d <- surveycore::as_survey(
  data.frame(
    q1 = sample(c("Agree", "Neutral", "Disagree"), 200, replace = TRUE),
    wt = runif(200, 0.5, 1.5)
  ),
  weights = wt
)
export_topline(d, vars = q1, file_name = "topline.xlsx")
}

}
\seealso{
\code{\link[surveycore:get_freqs]{surveycore::get_freqs()}} for the underlying frequency function.
}
\concept{frequency functions}
