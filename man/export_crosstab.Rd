% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-crosstab.R
\name{export_crosstab}
\alias{export_crosstab}
\title{Export a crosstab frequency table to an Excel workbook}
\usage{
export_crosstab(
  design,
  vars,
  banner,
  file_name,
  layout = c("per_question", "stacked"),
  interactions = NULL,
  pub_type = c("none", "external", "internal"),
  variance = NULL,
  conf_level = 0.95,
  show_n = TRUE,
  show_eff_n = FALSE,
  decimals = 1L,
  show_total = TRUE,
  base_notes = NULL
)
}
\arguments{
\item{design}{A survey design object created by
\code{\link[surveycore:as_survey]{surveycore::as_survey()}}, \code{\link[surveycore:as_survey_replicate]{surveycore::as_survey_replicate()}}, or
\code{\link[surveycore:as_survey_twophase]{surveycore::as_survey_twophase()}}. A
\link[surveycore:survey_collection]{surveycore::survey_collection} is \strong{not} accepted; use
\code{\link[=export_topline]{export_topline()}} for wave-comparison (trend) output.}

\item{vars}{<\code{\link[tidyselect:language]{tidy-select}}> Variable(s) to
tabulate. Use bare column names (\code{q1}), \code{c(q1, q2)}, or tidyselect
helpers such as \code{starts_with("q")}.}

\item{banner}{<\code{\link[tidyselect:language]{tidy-select}}> Banner (column)
variable(s). Each unique level of each banner variable becomes a
column in the crosstab table. Use bare column names or tidyselect
helpers.}

\item{file_name}{Path to the output \code{.xlsx} file. Must end in \code{.xlsx}.}

\item{layout}{One of \code{"per_question"} (default) or \code{"stacked"}.
Controls whether each question gets its own worksheet or all questions
are stacked on a single sheet.}

\item{interactions}{Optional list of character vectors. Each element
names two or more variables from \code{banner} whose levels will be crossed
(interacted) to form additional crosstab columns. Default \code{NULL}.}

\item{pub_type}{One of \code{"none"} (default), \code{"external"}, or
\code{"internal"}. Controls suppression thresholds for small subgroups.
\code{"none"} disables suppression; \code{"external"} suppresses subgroups with
effective or raw N < 100; \code{"internal"} uses a threshold of 50 effective
N or 100 raw N.}

\item{variance}{One of \code{NULL}, \code{"se"}, or \code{"ci"}. Controls whether
standard errors or confidence intervals are shown. Default \code{NULL}.}

\item{conf_level}{Numeric scalar in \verb{(0, 1)}. Confidence level for
intervals when \code{variance = "ci"}. Default \code{0.95}.}

\item{show_n}{Logical. Whether to include an unweighted count row.
Default \code{TRUE}.}

\item{show_eff_n}{Logical. Whether to display the effective N in column
headers. Default \code{FALSE}.}

\item{decimals}{Positive integer. Number of decimal places for displayed
percentages. Default \code{1L}.}

\item{show_total}{Logical. Whether to render a \code{Total} column (the
full-sample distribution) before the banner columns in every table.
Default \code{TRUE}.}

\item{base_notes}{Optional named character vector mapping variable names
to base descriptions (e.g.
\code{c(q5 = "Base: adults who donated (n=425)")}). When a rendered table's
first variable has an entry, its text is written as an italic row
directly under the question title. For sata/battery groups the lookup
key is the group's first member. Default \code{NULL} (no base rows).}
}
\value{
\code{invisible(file_name)} -- the path supplied in \code{file_name}.
}
\description{
Produces a publication-quality crosstab frequency table for one or more
survey variables cross-tabulated against one or more banner variables,
written to a styled \code{.xlsx} workbook. Variables are auto-classified
(single-choice, SATA, or battery) via
\code{\link[surveycore:classify_question_type]{surveycore::classify_question_type()}} and rendered according to
\code{layout}.
}
\examples{
\dontrun{
df <- data.frame(
  q1    = sample(c("Agree", "Neutral", "Disagree"), 200, replace = TRUE),
  group = sample(c("A", "B"), 200, replace = TRUE),
  wt    = runif(200, 0.5, 1.5)
)
d <- surveycore::as_survey(df, weights = wt)
export_crosstab(d, vars = q1, banner = group, file_name = "crosstab.xlsx")
}

}
\seealso{
\code{\link[=export_topline]{export_topline()}} for topline (no banner) output,
\code{\link[surveycore:get_freqs]{surveycore::get_freqs()}} for the underlying frequency function.

Other frequency functions: 
\code{\link{export_topline}()}
}
\concept{frequency functions}
